import { Router } from 'express';

const menteeRoutes = Router();

// Protected mentee routes will be added here
// Example: menteeRoutes.get('/dashboard', menteeAuthMiddleware, menteeController.getDashboard);

export default menteeRoutes;